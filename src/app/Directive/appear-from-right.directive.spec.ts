import { AppearFromRightDirective } from './appear-from-right.directive';

describe('AppearFromRightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearFromRightDirective();
    expect(directive).toBeTruthy();
  });
});
