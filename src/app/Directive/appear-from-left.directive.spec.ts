import { AppearFromLeftDirective } from './appear-from-left.directive';

describe('AppearFromLeftDirective', () => {
  it('should create an instance', () => {
    const directive = new AppearFromLeftDirective();
    expect(directive).toBeTruthy();
  });
});
